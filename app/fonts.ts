import {Geologica, Inter, Noto_Sans_SC, Rubik, Saira} from 'next/font/google';

export const saira = Saira({
  subsets: ['latin'],
  display: 'swap',
})
export const noto_sc = Noto_Sans_SC({
  subsets: ['cyrillic'],
  display: 'swap',
});
export const rubik = Rubik({
  subsets: ['latin'],
  display: 'swap',
});
export const geologica = Geologica({
  subsets: ['latin'],
  display: 'swap',
});